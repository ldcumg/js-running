function 함수명(매개변수) {
    // 코드를 작성해 주세요.
    let a = 매개변수 % 2
    if (a === 0) {
        return "짝수"
    } else {
        return "홀수"
    }
}

console.log(함수명(10)); // 결과값 "짝수";
console.log(함수명(7)); // 결과값 "홀수";
